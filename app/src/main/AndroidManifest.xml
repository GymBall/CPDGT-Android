<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.gymball.cpdgt_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.gtmode.gtlecture.GTLectureNewActivity"></activity>
        <activity android:name=".ui.gtmode.gtlecture.GTLectureModify" />
        <activity android:name=".ui.gtmode.GTModeActivity" />
        <activity android:name=".ui.lecturedetail.ChatActivity" />
        <activity android:name=".ui.lecturedetail.LectureDetailActivity" />
        <activity android:name=".ui.lecture.LectureCommentActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.join.JoinActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>